services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: norfolk
      POSTGRES_PASSWORD: norfolk
      POSTGRES_DB: norfolk_dev
    volumes:
      - norfolk_pg:/var/lib/postgresql/data

volumes:
  norfolk_pg:
